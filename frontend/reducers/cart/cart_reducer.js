
const cartReducer = (state = {}, action) => {
    Object.freeze(state);

    switch (action.type) {
        // case RECEIVE_CART:
            
        //     break;
    
        default:
            return state;
    }


}
export default cartReducer;